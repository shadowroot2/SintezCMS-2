// ПРОВЕРКА E-MAILvar email_check = /^([a-z0-9_\-]+\.)*[a-z0-9_\-]+@([a-z0-9][a-z0-9\-]*[a-z0-9]\.)+[a-z]{2,4}$/i;// ВЫБРОС ОШИБОКfunction error_alert(msg){	alert('Ошибка: '+msg);}// str_replacefunction str_replace(haystack, needle, replacement){	var temp = haystack.split(needle);	return temp.join(replacement);}// DOM$(function(){	// ОЧИСТКА ПОЛЕЙ	$('.needClear').focus(function()	{		if ($(this).val() == $(this).data('val')) $(this).removeClass('placeholder').val('');	})	.blur(function()	{		if ($(this).val() == '') $(this).addClass('placeholder').val($(this).data('val'));	});	// ВВОД ЦИФР	$('.digInput').keyup(function()	{		$(this).val($(this).val().replace(/[^\d]/g, ''));	});	// МОДАЛЬНЫЕ ОКНА	$('a.modal_lnk[href^=#]').click(function()	{		$('.modal_win.active').hide();		$('#modal_overlay').remove();		var $win = $('.modal_win[rel="'+$(this).attr('rel')+'"]');		$win.css('margin-top', -($win.height() / 2)).addClass('active');		$('body').append('<div id="modal_overlay"></div>');		$('#modal_overlay').fadeIn(700);		$win.fadeIn(700);		return false;	});	$('.modal_win .close, #modal_overlay').on('click', function()	{		$('.modal_win.active, #modal_overlay').fadeOut(500, function()		{			$('#modal_overlay').remove();		});		return false;	});	// FANCYBOX	$('a.fancy').fancybox();});